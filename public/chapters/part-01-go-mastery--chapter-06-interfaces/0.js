export default `# Chapter 6: Interfaces, The Heart of Go

*"If C++ and Java are about type hierarchies and the taxonomy of types, Go is about composition. The bigger the interface, the weaker the abstraction."* - Rob Pike

Interfaces are the mechanism through which Go achieves the two goals that most languages force you to trade off against each other: compile-time type safety and runtime flexibility. In Java or C#, you get safety but pay for it with explicit \`implements\` declarations that create tight coupling between producers and consumers. In Python or Ruby, you get flexibility through duck typing but lose compile-time guarantees, so interface mismatches become runtime errors discovered in production rather than compiler errors discovered in your editor. Go's implicit interface satisfaction gives you both. A type satisfies an interface simply by having the right methods, with no declaration required, and the compiler verifies the contract at every assignment boundary. This single design decision shapes how Go programs handle dependency inversion, testability, and the ability to evolve interfaces independently from the types that implement them.

The most important interface design principle in Go is also its most counterintuitive for developers coming from object-oriented languages: interfaces should be small. The \`io.Reader\` interface has exactly one method. \`io.Writer\` has one method. \`io.Closer\` has one method. These single-method interfaces are then composed (\`io.ReadWriter\`, \`io.ReadCloser\`, \`io.ReadWriteCloser\`) into precisely the combinations that different consumers need. This is not minimalism for aesthetic reasons. It is an architectural pattern called the Interface Segregation Principle applied at the language level. Small interfaces are easy to implement, easy to mock in tests, and easy to reason about. A \`UserRepository\` interface with fifteen methods is hard to fake for testing and hard to swap for a different implementation. A \`UserFinder\` interface with one method (\`FindByID\`) can be implemented by a real database, an in-memory cache, a test stub, and a circuit-breaker wrapper, all interchangeably, all without touching the code that depends on it.

The nil interface pitfall catches experienced developers regularly. A nil pointer of a concrete type is not the same as a nil interface value. When you store a nil \`*MyError\` pointer in an \`error\` interface, the interface value is not nil. It holds a non-nil type descriptor alongside a nil value pointer, so \`if err != nil\` evaluates to \`true\` even though the underlying pointer is nil, and dereferencing it panics. This behavior is a direct consequence of how interface values are implemented as two-word pairs (type, value) in the Go runtime, and understanding it at that level is the only way to reliably avoid it. This chapter explains that layout from first principles, turning the pitfall into a predictable behavior you can reason about rather than a source of intermittent production panics.

**What you will learn in this chapter:**

- Implicit interface satisfaction, how Go's structural typing works internally, and why it enables architectural patterns impossible in nominally typed languages
- Designing small, focused interfaces, applying the Interface Segregation Principle, the single-method interface pattern, and the standard library's composition model
- Interface values as (type, value) pairs, the internal two-word representation, how it determines nil behavior, assignment semantics, and performance implications
- The nil interface pitfall, why returning a nil \`*MyError\` as an \`error\` interface causes \`err != nil\` to be true, and the correct patterns to avoid this class of bug
- Interface composition, embedding interfaces within interfaces, the standard library's \`io\` package as a design blueprint, and building composable behavioral contracts
- The empty interface (\`interface{}\` / \`any\`), legitimate uses, performance costs, type assertion overhead, and why its use should be minimized in well-typed code
- Interface-based testing, writing code that is inherently testable through interface boundaries, using \`testify/mock\`, hand-written fakes, and the seams pattern
- Performance of interfaces, the method dispatch cost, when to prefer concrete types in hot paths, and how the compiler devirtualizes interface calls in some cases

**Why this matters at scale:**

Google's Go codebase style guide emphasizes interface design as one of the top three factors in long-term maintainability, alongside error handling and concurrency patterns. At Uber, the dependency injection framework used across thousands of Go services is built on top of \`uber-go/fx\`, which relies entirely on interface-based dependency inversion to make services independently testable and deployable. Netflix's Go services use interface boundaries as the explicit seam between business logic and infrastructure (databases, caches, external APIs), a pattern that allowed them to migrate storage backends for their metadata services without changing a line of business logic. Stripe's payments platform uses interfaces to implement multi-provider payment processing. The same \`PaymentProcessor\` interface is satisfied by Stripe's own implementation, by test doubles in unit tests, and by third-party provider adapters, with the compiler verifying that every implementation is complete and correct.

**Prerequisites:** Chapters 1-5, particularly the type system coverage in Chapter 4. Understanding of basic struct definitions and method syntax.

> **For readers new to programming:** interfaces feel abstract on first reading. The mental model that helps is "an interface is a contract, and any type with the right methods satisfies it". Work through Sections 5.1, 5.2, and 5.3 and write a small program that passes a concrete type through an interface parameter. The internals (Section 5.6) and the design principles (Section 5.5) can wait until you have used interfaces for a few weeks.
>
> **For readers already senior at a FAANG-equivalent company:** Sections 5.5 (design principles) and 5.9 (testing with interfaces) are the ammunition for the architecture review where a staff engineer proposes a fifteen-method \`Service\` interface. The "accept interfaces, return structs" discipline and the "small interfaces close to the consumer" rule are the patterns this chapter makes the case for.

**Chapter navigation by career stage.** Three readers, different sections.

- **Junior or self-taught engineer (targeting FAANG-entry):** your goal is a correct mental model of implicit interface satisfaction, the nil-interface pitfall, and how to satisfy \`io.Reader\` or \`error\` with your own types. Sections 5.1 through 5.4 are the core. The interview bar at Go-team roles expects you to explain "why does Go use structural typing" without hedging, to diagnose a typed-nil bug on sight, and to write a small interface and a type that satisfies it in three minutes.
- **Mid-level engineer crossing into Go from an OO language:** your trap is importing Java-style or C#-style interface patterns. A \`Service\` interface with fifteen methods, one interface per class, and \`I\`-prefixed names are all anti-patterns in Go. Read Section 5.5 (design principles) carefully. The small-interface discipline is the single biggest mindset shift from Java to Go.
- **Senior / Staff / Principal at a FAANG-equivalent:** the design material (5.5) and the interface-driven application walkthrough (5.9) are the reference material you adapt for the team's architecture-review checklist. The "accept interfaces, return structs" rule, the "define interfaces close to the consumer" rule, and the "interface segregation at the package boundary" rule are what you reach for when a design document proposes a monolithic service interface.

**What the senior track gets in this chapter that most Go interface material skips.** Standard Go interface content stops at "here is how structural typing works". This book adds, at every section: the dependency-direction framing (interfaces defined in the consumer package invert the usual dependency arrow), the testability framing (small interfaces are easy to fake, large ones are not), the evolution framing (adding a method to an interface is a breaking change, and the team's discipline determines how that evolves), and the performance framing (interface dispatch has a cost, and the hot path sometimes needs concrete types). The chapter is calibrated to senior engineers who own the architectural decisions for Go services.

**Staff and Principal lens: interfaces as organizational contracts.** At the staff level and above, an interface is not just a compile-time artifact. It is the smallest atomic unit of cross-team coupling in the codebase. When Team A defines a \`PaymentProcessor\` interface that Team B implements, the interface is the contract that lets both teams ship independently. Every method added is a new obligation for every implementer, every method renamed is a coordinated migration across repos, and every method removed is a deprecation campaign that can span quarters. The staff-level discipline is to treat the public interface surface of a Go package the way API designers treat a wire protocol. Versioning strategies (parallel interfaces, method-at-a-time rollouts, \`internal\` packages to limit blast radius) belong in the team's written standards, not in tribal knowledge. Principal engineers own the multi-year evolution story. How does the \`Storage\` interface change when the company moves from one region to three, from one storage backend to a federation of four, from synchronous calls to event sourcing? The answer is rarely "rewrite the interface". It is usually "extend through composition, deprecate through parallel rollout, migrate through a one-way adapter". This chapter lays the groundwork for those decisions. It does not substitute for the RFC process, but it gives you the vocabulary and constraints to run one.

**Go 1.26 note.** Type parameters (generics, Go 1.18 onwards) change the interface design conversation in a specific way: interfaces can now express type sets (\`interface { ~int | ~int64 }\`) used as constraints rather than as runtime contracts. This does not replace method-set interfaces. The two serve different purposes. Runtime polymorphism still goes through method-set interfaces. Compile-time type constraints go through type-set interfaces. Section 6.5 and Section 6.7 cover both, and the staff-track guidance is the same: prefer the smallest interface that compiles, whether it is one method or one type constraint.

---
`;
