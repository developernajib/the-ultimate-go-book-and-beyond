export default `## Learning Objectives

1. **Call C functions from Go** using CGO with correct memory management
2. **Call Go functions from C** using \`//export\` correctly
3. **Manage memory ownership** across the Go/C boundary without leaks or double-frees
4. **Understand CGO overhead** and decide when it is worth paying
5. **Read Plan 9 assembly** generated by the Go compiler
6. **Write a basic assembly function** that replaces a Go function in a hot path
7. **Use SIMD intrinsics** via assembly for data-parallel operations

### Detailed Outcomes

**Mid-level engineer**

- Recognise CGO in code and articulate its operational cost.
- Evaluate whether a CGO dependency is worth the complexity.

**Senior engineer**

- Review CGO PRs and reject or approve with evidence.
- Identify CGO in existing codebases that could be replaced with pure Go.
- Manage the cross-compilation story when CGO is required.

**Staff or Principal engineer**

- Set the org-wide CGO policy (allowed, restricted, banned per service class).
- Evaluate whether a new service should avoid CGO entirely to preserve Go's operational simplicity.

---
`;
