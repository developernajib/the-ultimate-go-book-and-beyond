export default `# Chapter 5: Pointers and Memory

*"Go's pointers are simpler than C's. There is no pointer arithmetic, so you cannot accidentally walk off the end of a buffer. But they are real pointers, understanding them is the difference between writing Go and writing fast Go."* - Rob Pike

Most Go developers learn enough about pointers to make code compile, then stop. They know \`&\` takes an address and \`*\` dereferences one, they know nil pointers panic, and they have a vague sense that pointer receivers are faster for large structs. That surface knowledge is enough to write correct Go programs in a narrow happy path. But it is not enough to understand why a service that looks correct under low load starts exhibiting latency spikes at 10,000 requests per second, spikes that turn out to be GC pressure caused by thousands of small heap allocations that could have stayed on the stack with a simple code change. Memory behavior is where the difference between competent Go developers and senior Go engineers shows most clearly, because memory mistakes are silent until they are catastrophic.

Go's memory model is built on a clean division between the stack and the heap. The stack is fast, automatically managed, and grows and shrinks with function call depth, no GC involvement required. The heap is where longer-lived allocations live, managed by Go's concurrent tri-color mark-and-sweep garbage collector. Since Go 1.26 (February 2026), the default collector is Green Tea, which restructures the marking phase around small-object groups for better cache locality and SIMD parallelism. Production workloads typically see 10 to 40 percent lower GC overhead after the 1.26 upgrade with no code changes. The tri-color invariant and the escape-analysis rules in this chapter remain unchanged. The collector just runs faster. The critical insight is that Go's compiler decides which allocations go where through a process called escape analysis: if a variable's lifetime can be proven to not outlast the function that creates it, it stays on the stack and is freed for free when the function returns. If a variable "escapes", because it is returned by pointer, stored in a heap-allocated struct, captured by a goroutine, or passed to an interface, it moves to the heap and the GC must eventually collect it. Understanding what causes escapes, and how to prevent unnecessary ones, is the core skill this chapter develops.

The practical stakes are significant. Uber's Go infrastructure team published post-mortems on multiple incidents where excessive heap allocations caused GC pauses that cascaded into latency SLA breaches at peak traffic. Netflix's Go services team has written extensively about redesigning hot-path data structures to minimize heap pressure in their streaming metadata service, which handles billions of lookups per day. Cloudflare optimized their Go-based DNS resolver by eliminating allocations in the per-request code path entirely, moving to a pool-based design with \`sync.Pool\` - and reduced p99 latency by 40%. These are not exotic optimizations. They are the natural result of understanding the stack/heap boundary deeply enough to make intentional decisions rather than accidental ones.

**What you will learn in this chapter:**

- Stack vs heap allocation, how Go decides where variables live, what the costs of each are, and how to reason about memory lifetime
- Escape analysis in depth, running \`go build -gcflags="-m"\` to inspect allocation decisions and interpreting the output to find optimization opportunities
- Pointer receivers vs value receivers, the performance trade-offs, the correctness implications, and the idiomatic rules for choosing between them
- \`unsafe.Pointer\` - when and how to use it safely, what guarantees it breaks, and the patterns (like reading struct fields by offset) that justify the risk
- Memory alignment and struct padding, how the CPU's alignment requirements cause invisible padding bytes, how field order affects struct size, and how to use \`unsafe.Sizeof\` and \`unsafe.Offsetof\` to audit layouts
- \`sync.Pool\` - how to use object pools to amortize allocation costs on hot paths, the GC interaction semantics, and when pooling helps vs hurts
- nil semantics, the nil zero value, nil interface vs nil pointer distinctions, how nil receivers work, and the most common nil-related bugs in production Go code
- Memory profiling with \`pprof\` - capturing heap profiles, reading allocation flame graphs, and using \`runtime/trace\` to diagnose GC pauses in running services

**Why this matters at scale:**

Memory management is the silent determinant of whether a Go service handles 10,000 requests per second or 100,000. At Uber, the team responsible for their Go platform wrote a custom allocator for a high-frequency messaging path after profiling showed that standard heap allocation was their primary bottleneck, not business logic, not network I/O, but allocation overhead. Google's gRPC-Go implementation was heavily optimized across multiple release cycles specifically to reduce per-RPC allocations, with each reduction translating directly to lower GC pause frequency at their traffic scale. Cloudflare's \`1.1.1.1\` DNS resolver, which answers hundreds of millions of queries per day, operates with a near-zero allocation budget in its hot path. Learning to see your program through the lens of memory, where values live, how long they live, and what collecting them costs, is what separates engineers who write Go from engineers who write fast, production-grade Go.

**Prerequisites:** Chapters 1 to 3. Basic understanding of what a pointer is (any language background is fine). No prior knowledge of C memory management required.

> **For readers new to programming:** the surface skill (what \`&\` does, what \`*\` does, what nil is) is enough for a first pass. The internals (escape analysis, stack-vs-heap, GC interaction, \`sync.Pool\`) can wait until you have a real program that performs poorly. Read the fundamentals sections (5.1 through 5.3, 5.5, 5.6) and skim the rest. Come back for the deep dive once you have stood in front of a pprof flame graph that you did not understand.
>
> **For readers already senior at a FAANG-equivalent company:** this chapter is the one you have been waiting for. Escape analysis, memory layout, \`sync.Pool\` mechanics, and the \`unsafe\` subset Go makes available are the levers you reach for when a service does not meet its latency budget. The sections on Green Tea GC (1.26) and the allocation profile reading in Section 5.9 are calibrated for staff-plus engineers who own service performance.

**Chapter navigation by career stage.** This chapter serves three different readers, and the sections that pay off differ.

- **Junior or self-taught engineer (targeting FAANG-entry):** your goal is a correct mental model of pointers, nil, and value vs pointer receivers. Sections 5.1 (pointer fundamentals), 5.2 (when to use pointers), 5.5 (understanding nil), and 5.6 (value vs pointer receivers) are the on-ramp. The interview bar at Go-team roles at Google, Meta, and Stripe expects you to explain the nil-interface-vs-nil-pointer distinction, to pick the correct receiver type for a new method, and to explain what happens when you dereference a nil pointer. All three are covered here. The stack-vs-heap and escape-analysis material is bonus for juniors, but a phone screen could go there for a strong candidate.
- **Mid-level engineer moving performance-sensitive code:** your goal is to read the output of \`go build -gcflags="-m"\`, to interpret a pprof heap profile, and to write a \`sync.Pool\` correctly. Sections 5.3 (stack vs heap), 5.4 (escape analysis), 5.8 (memory profiling), and 5.9 (sync.Pool) are the core. The discipline shift from "make it work" to "make it allocate less" is where mid-level engineers grow most in their first year on a Go team.
- **Senior / Staff / Principal at a FAANG-equivalent:** the internals are the point. The Green Tea GC behaviour in 1.26, the escape-analysis rules that bite in specific patterns (interface boxing, closure capture, map-value-address), and the \`sync.Pool\` GC interaction that can bite teams that trust the pool to keep arbitrary objects. Section 5.10 (complete memory-management application) is the worked example you can adapt for the team's reference design, and Section 5.11 (common mistakes) is the skeleton of a team review checklist.

**What the senior track gets in this chapter that most Go memory material skips.** Standard Go memory content stops at "here is a pointer, here is the heap". This book adds, at every section: the pprof framing (what symbols show up when this pattern is wrong), the GC framing (how the allocation interacts with the collector's workload), the incident framing (what production shapes this pattern produces, from Uber, Netflix, and Cloudflare post-mortems), and the review-time framing (what a staff reviewer catches in a PR that touches allocation-hot code). The 1.26 Green Tea GC changes, the \`weak.Pointer[T]\` addition (1.24), and the \`runtime.AddCleanup\` mechanism (replacing \`runtime.SetFinalizer\`) are all covered at the level a senior engineer running production Go services in 2026 needs.

**A note on currency.** Every code sample and benchmark in this chapter is verified against Go 1.26. Where behaviour changed between versions we say so explicitly, especially for the Green Tea GC (1.26), the \`unique\` package (1.23), the \`weak\` package (1.24), and the loop-variable semantics (1.22). The GC behaviour in particular has changed enough between 1.20 and 1.26 that older blog posts and Stack Overflow answers are often stale. Trust the release notes and the \`runtime\` package docs over folklore.

---
`;
