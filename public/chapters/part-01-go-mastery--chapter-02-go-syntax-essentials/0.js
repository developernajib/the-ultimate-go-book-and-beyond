export default `# Chapter 2: Go Syntax Essentials

Before tackling Go's deeper features, you need to be comfortable writing basic Go code. This chapter gives you the hands-on foundation: how Go programs are structured, how variables and types work, how to write functions, and how to use Go's core data structures. By the end of this chapter you should be able to read an unfamiliar Go file written by another engineer and understand what it does, even if you have never seen the package before. That is not an accident of Go's design. The language was deliberately built so that the syntax surface a working engineer needs to recognise is small enough to fit in their head, and the rest of this book assumes you already have that surface internalised.

If you already know Go basics, skip ahead to Chapter 3. If you're coming from another language (Python, Java, JavaScript, C), this chapter will get you writing Go in under an hour. If you've never programmed before, take your time here, every concept in the rest of the book builds on this foundation. The chapter is intentionally light on theory and heavy on small, runnable programs. Go rewards reading code more than reading prose, and the interview bar at FAANG-equivalent companies is calibrated against engineers who can sit down at a fresh terminal and produce a correct twenty-line program in five minutes.

**What this chapter covers.** Package declaration and the \`main\` entry point. The four ways to declare a variable and when each is idiomatic. Go's built-in types and zero values. Function signatures, multiple return values, and named returns. Control flow primitives \`if\`, \`for\`, \`switch\`, and the deliberate absence of \`while\`. Structs, methods, value vs pointer receivers, and why this distinction matters more in Go than in most languages. Arrays vs slices vs maps, including the slice-vs-array confusion that trips up almost every newcomer. Idiomatic error handling with the \`error\` interface, sentinel errors, \`errors.Is\`, \`errors.As\`, and \`%w\` wrapping introduced in Go 1.13 and stabilised through 1.26.

**What this chapter does not cover.** Concurrency (goroutines, channels, \`select\`) is Chapter 7. Generics, the type system in depth, and interface satisfaction rules are Chapter 4. The runtime, garbage collector, and scheduler internals are Chapter 7. We are deliberately staying inside the syntax surface here so that later chapters can assume you can read code without stopping to look up \`:=\` versus \`=\` or what a nil slice prints.

**Prerequisites:** Go 1.26 (or any 1.22+) installed on your machine (see Chapter 1, Section 1.7). A text editor or IDE (VS Code with the Go extension, GoLand, or Neovim with \`gopls\` configured). A working \`go mod init\` in a scratch directory you can throw away. We assume you have already run \`go run hello.go\` at least once from Chapter 1, Section 1.8. If you have not, do that first, because the rest of this chapter assumes you can put code in a file and execute it without help.

> **For readers new to programming:** work through every section in order and type the examples by hand. Do not copy-paste. Muscle memory matters more than speed on this chapter. When a section says "try changing X and see what happens", actually do it. The compiler error messages are part of the learning, and Go's compiler is unusually good at telling you exactly what is wrong in plain English.
>
> **For readers already fluent in Go:** you can skim this chapter and jump straight to Chapter 3 (mental model shifts) or Chapter 4 (types deep dive). The interview-questions file at the end of this chapter is still worth a glance, because entry-level FAANG phone screens often cover exactly this material and a senior engineer interviewing a junior will ask them. The structs-and-methods section also has a short note on receiver-type consistency that catches even experienced Go engineers in code review, so it is worth a thirty-second skim.

**Chapter navigation by career stage.** The same syntax surface serves three different readers, and the parts that pay off are not the same for each.

- **Junior or self-taught engineer (targeting FAANG-entry):** treat this chapter as your fluency baseline. The phone-screen and on-site bar at Google, Meta, Amazon, and Stripe for Go-team roles assumes you can write a function, declare a struct, iterate over a map, and return an error in one sitting without a reference. Type every example. Then close the book and reproduce a small program (a word-count CLI, a CSV parser, a temperature converter) from memory. If you cannot, you are not ready for the interview, no matter how much theory you have read. Pay particular attention to slices (Section 2.6) and error handling (Section 2.7) because those two topics together account for the majority of "easy" Go coding questions and the majority of "easy" Go production bugs.
- **Mid-level engineer crossing into Go (3 to 7 years in another language):** the trap at this level is assuming Go is "Java without the boilerplate" or "Python with types". Both framings are wrong and they will cost you in code review. Read Section 2.5 (structs and methods) carefully because Go's composition-over-inheritance model is the single biggest mental shift coming from Java or C#. Read Section 2.7 (error handling) because the fact that errors are values, not exceptions, changes how you structure functions, not just how you handle failure. The five minutes you save by skimming this chapter will cost you a week in PR comments.
- **Senior / Staff / Principal at a FAANG-equivalent (the audience most syntax chapters ignore):** you already know the syntax. The value here for you is in the meta-pattern: this chapter is a worked example of how to teach a syntax surface to engineers you onboard. When you bring a Python or Java engineer onto a Go service, you are going to retell most of what is in this chapter in some form, and the order in which you introduce concepts (package then types then functions then methods then errors, never the reverse) is itself a reusable artifact. Use the section structure as the skeleton of your team's internal Go onboarding doc. Two specific items are also worth your time as a senior reviewer: the receiver-type consistency note in Section 2.5 (because mixed value and pointer receivers on the same type is a recurring code-review finding even on staff-led projects), and the error-wrapping discipline in Section 2.7 (because consistent use of \`%w\` versus \`%v\` versus sentinel errors is the difference between an observability story that works at scale and one that produces unactionable error logs).

**What the senior track gets in this chapter that most Go books skip.** Standard syntax chapters stop at "here is a \`for\` loop, here is a struct". This book adds, at every section: the code-review framing (what a staff-level reviewer at a Go-heavy org would flag in this snippet), the migration framing (how this concept differs from the closest Python, Java, or TypeScript equivalent and where engineers from those backgrounds reliably get it wrong), and the operational framing (how the choice plays out at scale, for example why pointer receivers on hot-path methods change escape analysis and allocation profiles in ways that show up in pprof). If you are running a Go onboarding programme for a team of fifteen engineers, the dual-track structure of this chapter is the structure your internal training should mirror.

**A note on Go version currency.** Every code sample in this chapter has been verified against Go 1.26 (released February 2026). Where a feature was added in a specific version we say so explicitly, for example range-over-function iterators (1.23), the \`min\` and \`max\` builtins (1.21), and the standardised structured logging package \`log/slog\` (1.21). If you are on an older Go version, the differences are noted inline rather than in a separate appendix, because the gap between "this works on 1.20" and "this works on 1.26" is exactly the kind of thing that bites you during an interview live-coding round.
`;
