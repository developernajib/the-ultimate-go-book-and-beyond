export default `# Chapter 4: Types Deep Dive

*"A type is not just a constraint on values, it is a statement of intent. When you name a type, you are documenting what the value means, not just what it is."* - The Go Team

Go's type system occupies a deliberate middle ground that most languages miss. C gives you raw memory with no safety guarantees. Java buries every primitive in a class hierarchy and forces you to choose between \`int\` and \`Integer\` depending on whether nullability is needed. Haskell's type system is so expressive it can encode entire program proofs, but few engineers can read it fluently. Go's type system is simple enough to fully understand in a day, yet expressive enough to prevent entire categories of bugs that routinely escape review in dynamically typed codebases. Named types, value semantics, zero values, and structural embedding give you precision without ceremony, a combination that pays compounding returns as codebases grow.

The zero-value guarantee is one of Go's most underappreciated design decisions. Every type in Go has a well-defined zero value: integers start at \`0\`, booleans at \`false\`, strings at \`""\`, pointers at \`nil\`, and slices at an empty slice that is safe to \`append\` to. There is no uninitialized memory, no \`NullPointerException\` waiting to surface in production, no need to write boilerplate initialization for every struct field. This guarantee makes Go code more predictable than C, safer than Java (where object fields default to \`null\` and trigger NPEs), and more maintainable than Python (where unset attributes raise \`AttributeError\` at runtime). For teams maintaining services that must stay reliable at 99.99% uptime, zero values eliminate an entire category of initialization bugs that plague other languages.

Named types and embedding replace class hierarchies with something more flexible and composable. In Go, you define a named type over any underlying type, not just structs, which means \`type UserID int64\` and \`type OrderID int64\` are distinct types that cannot be accidentally mixed, even though both are \`int64\` internally. The compiler enforces the distinction, turning what would be a subtle runtime bug in other languages into a compile error caught immediately. Struct embedding, placing one type inside another without a field name, composes behavior by promoting the inner type's methods to the outer type, giving you the code reuse of inheritance without its tight coupling. When Stripe's Go team designed their payment domain model, named types for monetary amounts, currency codes, and account identifiers eliminated an entire class of cross-domain value confusion bugs that had caused incidents in their previous Ruby codebase.

**What you will learn in this chapter:**

- Go's complete numeric type hierarchy, when to choose \`int\` vs \`int64\` vs \`uint32\`, and how type width affects performance and correctness
- String internals, UTF-8 encoding, rune vs byte, the \`strings\` and \`unicode\` packages, and how to process multi-language text efficiently
- Slice mechanics from the inside out, the three-word header (pointer, length, capacity), \`append\` growth behavior, and how to avoid unintended memory sharing between slices
- Map internals, hash bucket layout, why map iteration is random, how to use maps safely in concurrent code, and the \`sync.Map\` trade-offs
- Named types and type aliases, how to encode domain semantics into the type system and let the compiler enforce business rules
- Struct design, field ordering for memory efficiency, struct tags for serialization, embedding for behavioral composition, and anonymous fields
- Zero values, designing types that are useful in their zero state, and patterns like the functional options pattern that take advantage of zero values cleanly
- Type assertions and type switches, how interface values carry dynamic type information and how to extract it safely

**Why this matters at scale:**

At Google, Uber, and Stripe, the type system is the first line of defense against an entire class of production bugs. Uber's Go style guide mandates named types for every identifier that represents a domain concept, driver IDs, rider IDs, trip IDs, fare amounts, precisely because Go's type system can catch accidental cross-domain value usage at compile time rather than in a production incident at 2 AM. Google's internal Go codebases use struct embedding extensively to compose HTTP middleware, database access layers, and RPC clients without the brittle inheritance trees that plagued their earlier Java services. Cloudflare's DNS resolver, written in Go, relies heavily on careful type design to maintain correctness across hundreds of record types. Their engineers have noted that the type system catches encoding mistakes that would otherwise produce malformed DNS responses silently.

**Prerequisites:** Chapters 1-3, or solid general programming experience. Familiarity with static typing concepts (Java, C#, TypeScript, or similar) is helpful but not required.

> **For readers new to programming:** this chapter goes deeper than Chapter 2's syntax surface. On the first pass, skim the internals sections (slice header layout, map bucket internals, memory alignment) and focus on the behavioural rules (when does \`append\` alias, what is the zero value of a map, why is iteration random). Come back for the internals once you have written Go for a few weeks and the behaviour has become intuition.
>
> **For readers already senior at a FAANG-equivalent company:** the internals sections are the point. Slice header layout, map bucket internals, and struct field alignment are the three places where a pprof investigation at 3 AM will send you, and being fluent in the underlying data representation cuts the debugging time in half. The named-types-for-domain-IDs pattern in Section 4.7 is also the single highest-leverage type-system discipline you can impose on a team, and the chapter makes the case you will need for the code-review argument.

**Chapter navigation by career stage.** This chapter serves three different readers, and the depth each needs is different.

- **Junior or self-taught engineer (targeting FAANG-entry):** your goal is fluency with Go's built-in types at the level an interviewer expects. Sections 4.1 through 4.6 (numbers, strings, arrays, slices, maps, structs) are non-negotiable. Sections 4.7 (type system advanced) and 4.8 (complete application) pay off in the on-site round when the interviewer asks you to model a domain. The single most common interview mistake from junior candidates is confusing slice aliasing semantics, and the single most common take-home-assignment mistake is writing to a nil map. Both are covered here with enough depth that you will recognise the failures when you see them.
- **Mid-level engineer coming to Go from another statically typed language:** your trap is importing patterns from your previous language that Go does not want. Java engineers reach for wrapper types. TypeScript engineers reach for generics everywhere. C++ engineers reach for templates plus operator overloading. Go has none of those, and the replacements (named types with methods, small interfaces, generics-when-justified) are the topic of Section 4.7. Read it carefully, because the code-review findings on your first Go PRs will almost all live there.
- **Senior / Staff / Principal at a FAANG-equivalent:** the internals sections are the point. If you have ever stared at a Go pprof flame graph looking at \`runtime.growslice\` or \`runtime.mapassign_fast64\`, this chapter is the text that makes those labels make sense. The staff-plus lesson of the chapter is that types are documentation, types are validation, and types are the cheapest place to enforce domain correctness. The \`UserID\` vs \`OrderID\` pattern in Section 4.7 is the easiest high-leverage win you can land in the first quarter at a new team, because it costs hours to implement and pays back forever in catching cross-domain value confusion before the code ever runs.

**What the senior track gets in this chapter that most Go type-system material skips.** Standard Go type content stops at "here are the built-in types, here are the standard operations". This book adds, at every section: the memory-layout framing (what the type looks like in memory, why that matters for cache locality and allocation profiles), the pprof framing (what symbols show up when this type is hot, and what to change), the domain-modelling framing (when to promote a primitive to a named type and when not to), and the API-design framing (what shape the public API should take so callers cannot misuse the type). The slice and map internals sections are calibrated to engineers who will be expected to diagnose performance pathologies in production.

**A note on currency.** Every code sample in this chapter is verified against Go 1.26 (released Q1 2026). Where a feature was added in a specific version we say so explicitly, for example generics (1.18), the standardised \`slices\` and \`maps\` packages (1.21), \`log/slog\` (1.21), per-iteration loop variables (1.22), range over function (1.23), and the \`tool\` directive in \`go.mod\` (1.24). Version differences matter for interviewing and for code review, because "this works on 1.21" and "this works on 1.26" are different answers to the same question.

---
`;
